## 数据挖掘
1. 数据挖掘
2. 数据预处理：从数据中*检测*/*纠正*或删除损坏或不准确和不适用于模型的记录；让数据更适应模型，匹配模型需求
3. 特征工程：将原始数据转换为*更能代表预测模型的潜在问题的特征*的过程，通过提取相关的特征或者创造特征实现；可能面对的问题有：有*无关的特征*；特征之间有*相关性*；特征太多或太小；目的是：降低**计算成本**，提升**模型上限**

##  数据预处理
### 数据无量纲化
将不同规格的数据转换到同一规格，转换到某个特定的分布。可以提升模型的精度，防止防止取值范围大的特征对模型产生影响。但是决策树不需要**无量纲化**，可以处理任何的数据。

包括线性和非线性的处理方法
- 线性的无量纲化包括**中心化**和**缩放处理**


#### 数据归一化
将数据按照最小值中心化，再按极差缩放，Normalization（不是正则化）
$$
x^{*} = \frac{x-\min (x)}{\max (x) - \min (x)}
$$
归一化后的数据服从**正态分布**。

模块 `sklearn.preprocessing.MinMaxScaler`
实例化对象后可以通过 `fit` 以及 `transform` 将数据归一化；如果数据量比较大，则可以使用 `partial_fit` 归一化

```py
scaler = MinMaxScaler()
scaler.fit(X)

X_scaled = scaler.transform(X)

# 还愿
X_returned = scaler.inverse_transform(X_scaled)
X_returned
```

或使用 `numpy`
```py
X_array = np.array(X)
X_scaled_numpy = (X_array - X_array.min(axis=0)) / (X_array.max(axis=0) - X_array.min(axis=0))

X_scaled_numpy * (X_array.max(axis=0) - X_array.min(axis=0))  + X_array.min(axis=0)
```

#### 数据标准化
数据按均值中心化，再按标准差缩放，变成均值为 0，方差为 1 的正态分布。

```py
from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
scaler.fit(X)

scaler.mean_, scaler.var_

X_standard = scaler.transform(X)
X_standard
```

在标准化和归一化时，**空值**（NaN）在 `fit` 时会被直接忽略，而在 `transform` 后也会保持 NaN 的形式；只允许导入*二维的数组*。

一般来说选用的是标准化来进行特征缩放。`MinMaxScaler` 对异常值敏感，过大的异常值会使得其他相对较小的数据被压缩的很小，因此一般用 `StandardScaler`。而 `MinMaxScaler` 对于不涉及距离度量/梯度/协方差计算或者数据需要被压缩到特定区间时适用。

中心化只需要 pandas 广播减去某个数即可。其他 `sklearn` 中的缩放处理模块有：
- `MaxAbsScaler` 希望数据压缩，同时不损失稀疏性（不影响取值为 0 的个数）
- `RobustScaler` 在数据噪声和异常值比较大时可用

#todo
![[image-6.webp]]


### 缺失值
使用 `sklearn.impute.SimpleImputer` 填充缺失值，包括 4 个重要的参数
- `missing_values` 缺失值时什么，默认是 `np.nan`
- `strategy` 填补缺失值的策略，有 `mean`，`median`，`most_frequent`，`constant`（需要配合 `fill_value`），*默认* `mean`
- `fill_value` 当参数 `strategy` 为 `constant` 时可用，表示填充的值，*默认 0*
- `copy` 默认为 True，表示创建一个副本

```py
from sklearn.impute import SimpleImputer

# 实例化类
imp_mean = SimpleImputer()
imp_median = SimpleImputer(strategy="median")
imp_0 = SimpleImputer(strategy="constant", fill_value=0)

# 先填补age列
Age = data.loc[:, "Age"].values.reshape(-1,1)
Age.shape

imp_mean = imp_mean.fit_transform(Age)
imp_median = imp_median.fit_transform(Age)
imp_0 = imp_0.fit_transform(Age)

# 选择用中位数填补
data.loc[:, "Age"] = imp_median
data.info()
```

对于 embarked 特征，为字符串型分类变量，用众数填补
```py
embarked = SimpleImputer(strategy="most_frequent")
embarked.fit_transform(data.loc[:, "Embarked"].values.reshape(-1,1))

data.loc[:, "Embarked"] = embarked
```

当然，也可以用 pandas 填补
```py
data_.loc[:, "Age"] = data_.loc[:, "Age"].fillna(data_.loc[:, "Age"].median())
```


### 分类型特征编码
#### LabelEncoder
处理标签，将分类转换为分类数值，模块为 `sklearn.preprocessing.LabelEncoder`

```py
# 标签的编码
y = X_copy.iloc[:, -1]

le = LabelEncoder()
le.fit(y)
```

在 fit 过后，可以查看标签中具体有多少的类别：
```py
le.classes_
```

当然也可以通过 `le.inverse_transform(label)` 逆转编码后的标签。

之后将标签转为编码后的值，当然也可以通过 `fit_transform` 来整合整个过程。
```py
label = le.transform(y)
# 取的是 sex 和 embarked 列
X_copy.iloc[:, -1] = label
X_copy.head()
```

#### OrdinalEncoder
对于特征，可以通过 `sklearn.preprocessing.OrdinalEncoder` 来进行编码。

```py
from sklearn.preprocessing import OrdinalEncoder

OrdinalEncoder().fit(X_copy_ordinal.iloc[:, 1:-1]).categories_
```
取出需要编码的特征列，在 fit 后可以通过 `.categories_` 查看每个特征具体有多少的类

最终通过 `transform` 实现特征列的编码：
```py
X_copy_ordinal.iloc[:, 1:-1] = OrdinalEncoder().fit_transform(X_copy_ordinal.iloc[:, 1:-1])
```

可以在结果中发现每个特征都编码为了**数字类型**，但是这种编码无法体现变量之间的联系。
一些常见的分类变量有：
- *名义变量*，如 Embarked 的取值 S Q C,彼此之间无联系
- *有序变量*，如学历的关系，但是不能加和和计算
- *有距变量*：如体重的划分分类：>45, >90, >135，*可以通过运算相互转换*

上述的变量编码后会变成一串有序的数字，模型会觉得某个特征中的变量通过运算可以得到一种等价关系，对于 Embarked 和学历特征来说会引入一些错误信息。

因此 `OrdinalEncoder` 适用于一些有序变量，对于名义变量，只能使用**哑变量**来处理。

#### OneHotEncoder
通过独热编码，将特征转化为哑变量，模块 `sklearn.preprocessing.OneHotEncoder`

先取出需要编码的特征列
```py
x = X_copy.iloc[:, 1:-1]
```
然后实例化对象，得到编码结果：
```py
enc = OneHotEncoder(categories="auto").fit(x)
result = enc.transform(x)

result.toarray()
```
转为array 后，可以发现将特征列OneHot编码后得到一个稀疏矩阵，一共5列，表示总的特征类别之和。

同理可以逆转编码
```py
pd.DataFrame(enc.inverse_transform(result))
```

当总的特征分类数比较多时，哑变量可能无法直观看出属于哪一个特征分类变量，可以通过：
```py
enc.get_feature_names_out()
```
查看哑变量的**每一个维度对应的特征取值**。（高版本）


最终，把哑变量的结果放入特征矩阵中，并且去掉原来的特征列，再重命名列，完成哑变量编码
```py
X_copy_onehot = pd.concat([X_copy, pd.DataFrame(result.toarray())], axis=1)
X_copy_onehot.drop(["Sex", "Embarked"], axis=1, inplace=True)

newX = X_copy_onehot.copy()
newX.columns = ["Age", "Survived", "Female", "Male", "C", "Q", "S"]
```

也可以把上面的 feature names out 的结果作为列名重命名。

标签是否可以做哑变量（可以 `LabelBinarizer`）？因为用的是分类算法（如决策树），因此没有必要对标签做哑变量编码。


### 连续性特征编码
#### 二值化
模块 `sklearn.preprocessing.Binarizer`
年龄变量是**连续性变量**，可以设定一个阈值，将年龄二值化。二值化是对文本计数数据的操作。

如：对年龄和存活情况进行二值化
```py
from sklearn.preprocessing import Binarizer

x = data_2.iloc[:, 0].values.reshape(-1,1)
transformer = Binarizer(threshold=30).fit_transform(x)
```
关于边界值的处理策略：
> Values greater than the threshold map to 1, while values less than or equal to the threshold map to 0. With the default threshold of 0, only positive values map to 1.

大于阈值的会被编码为 1.

#### 分箱
模块 `sklearn.preprocessing.KBinsDiscretizer`
将连续性变量划分为分类变量的类，

将年龄分为多个区间，分箱后编码，有如下的参数：
- `n_bins` **分箱的个数**（每个特征；一次会被运用到所有导入的特征，如果想每个特征的分箱个数不同，则需要一个个导入）
- `encode` **编码**的方式：默认是 `onehot` 返回一个稀疏矩阵，每一列的一个特征中的一个类别。
	- `ordinal` 每个特征的每个箱编码为一个整数，返回的矩阵中每一列是一个特征，每个特征下含不同整数编码的箱。
- `strategy` 定义**箱宽**的方式，默认是 `quantile`
	- `uniform`，表示*等宽分箱*。即每个箱的宽度为 `极差/n_bins`，每个箱的样本数是不均匀的。
	- `quantile` 表示*等位分箱*，特征箱内的样本数量相同。
	- `kmeans` 表示按聚类分箱。


```py
from sklearn.preprocessing import KBinsDiscretizer

X = data_2.iloc[:, 0].values.reshape(-1,1)
est = KBinsDiscretizer(n_bins=3, encode="ordinal", strategy="uniform")
est.fit(X)
est.transform(X)
pd.DataFrame(est.transform(X))[0].unique()

# one hot 分箱
est = KBinsDiscretizer(n_bins=3, encode="onehot", strategy="uniform")
est.fit(X)
est.transform(X).toarray()
```

## 特征选择
特征工程的内容：
- **特征提取**：从图像等非结构化数据中提取新的信息
- **特征创造**：将数据中的原有特征进行计算，得到一个新的特征。
- **特征选择**：从所有的特征中选择对模型有意义的。

做特征选择的第一步是理解业务。

```py
import pandas as pd
data = pd.read_csv("digit recognizor.csv")

X = data.iloc[:, 1:]
Y = data.iloc[:, 0]

X.shape
```
可以发现这个数据集的维度十分大，有 784 维，一般的算法难以处理，因此需要特征工程。

### Filter 过滤法
过滤法用作预处理的步骤。特征选择独立于机器学习算法。过滤法是根据各种统计指标来选择特征的。从所有的特征中选择最佳的特征子集。

#### 方差过滤
##### `VarianceThreshold`
通过特征本身的方差来筛选特征。
- 如果一个特征本身的方差很小，表明在这个特征中大多数的数值都一样。

因此要优先消除**方差为 0** 的特征。
```py
from sklearn.feature_selection import VarianceThreshold

selector = VarianceThreshold()
X_var0 = selector.fit_transform(X)

X_var0.shape
```
可以发现过滤前后的特征减少了 76 个。

查看过滤的特征：
```py
# 输入的特征
selector.feature_names_in_

# 输出的特征
selector.get_feature_names_out()

# 过滤的特征
selected = set(selector.feature_names_in_) - set(selector.get_feature_names_out())
```

此外可以根据方差的中位数过滤特征：
```py
x_fsvar = VarianceThreshold(np.median(X.var().values)).fit_transform(X)
```
若特征服从伯努利分布，则可以过滤某个分类中占到 80%以上的特征：
```py
x_bvar = VarianceThreshold(.8 * (1 - .8)).fit_transform(X)
```

接下来测试 KNN 算法和随机森林对于方差过滤后的运算效率：
```py
from sklearn.ensemble import RandomForestClassifier as RFC
from sklearn.neighbors import KNeighborsClassifier as KNN
from sklearn.model_selection import cross_val_score
import numpy as np

cross_val_score(KNN(), X, Y, cv=5).mean()
# 方差过滤后
cross_val_score(KNN(), x_fsvar, Y, cv=5).mean()

cross_val_score(RFC(n_estimators=10, random_state=42), X, Y, cv=5).mean()

cross_val_score(RFC(n_estimators=10, random_state=42), x_fsvar, Y, cv=5).mean()
```
发现：
- 随机森林的准确率（93.9%）不如 KNN 算法（96.6%）
- 但是随机森林的运行时间（16.3s）更少（27.6s）
- 发现方差过滤后 KNN 的运行时间变化很大（减小了约 6 s），而随机森林的变化很小（几乎没有变化，甚至可能增加）。（随机森林只选择若干个特征；而 KNN 需要遍历所有的特征，时间复杂度高）

因此，**过滤法**的主要对象是需要*遍历特征*或者*升维*的算法，主要目的是*维持算法表现的前提下，降低算法的计算成本*。

由于*集成算法*的特性，随机森林每次只选取*若干个特征*。传统的决策树需要计算每个特征的不纯度，虽然 sklearn 中的决策树也是随机选取特征计算的，但是由于选取的特征数远超过随机森林的，因此理论上，**过滤法会对决策树有作用但对随机森林无用**。

阈值对方差过滤效果的影响：
- 当**阈值过小**时，被过滤的特征比较少，*对模型的表现影响有限*；从*运行时间*来看：如果方差小的特征比较多，则会一定程度降低模型的运行时间。
- 当**阈值比较大**时，模型的表现可能变好（说明过滤的大多是噪声特征）也有可能变弱（过滤的是有效特征）；从*运行时间*来看：一定能够降低模型的运行时间，降低的效果取决于模型的时间复杂度。


##### 如何选择 threshold
虽然可以通过画学习曲线来判断，但实际上算法的运行时间可能过长，**不太现实**。因此常见的做法是：设定**阈值为 0 或者一个很小的阈值**，把一些*明显无用*的特征过滤，再用一些**更好的特征过滤方法**来减少特征的数量。因此，方差过滤通常是一种**预处理**的方式。

#### 相关性过滤
除了方差过滤，我们需要挑选出和标签相关的标签，因此需要考虑*不同特征和标签之间的相关性*，第一个重要的方法是卡方过滤。

##### 卡方过滤
卡方法过滤是针对**离散型**的标签的（*分类问题*）。相关的模块是 `feature_selection.chi2`

计算每个**非负**特征和标签之间的 $\chi^{2}$ 统计量，从高到低对特征进行排名。再通过 `feature_selection.SelectKBest` 设定评分标准选出前 K 个评分最高的特征。
注意特征要是非负的，因此需要先**归一化**到 01 之间的分布。

如果卡方检验发现某个特征中的值都是相同的，则会提示使用方差过滤。
```py
from sklearn.ensemble import RandomForestClassifier as RFC
from sklearn.model_selection import cross_val_score
from sklearn.feature_selection import SelectKBest, chi2
```

这里一共有 392 个特征，选择前 300 个相关性大的，再做交叉验证，计算过滤后模型的表现是否变差了：
```py
x_fschi = SelectKBest(chi2, k=300).fit_transform(x_fsvar, Y)
x_fschi.shape
```

```py
cross_val_score(RFC(n_estimators=10, random_state=42), x_fschi, Y, cv=10).mean()
```
可以发现 accuracy 降低了，因此可以推断我们过滤了有效的信息，即 K 值设的过小。有两个解决办法：
- 提升 K 值
- 放弃相关性过滤。

##### 如何选取 K
一个思路是通过画学习曲线来确定合适的 K，
```py
score = []

for i in range(350, 200, -10):
    x_fschi_i = SelectKBest(chi2, k=i).fit_transform(x_fsvar, Y)
    s = cross_val_score(RFC(n_estimators=10, random_state=42), x_fschi_i, Y, cv=5).mean()
    score.append(s)
    
plt.plot(range(350, 200, -10), score)
plt.show()
```
（需要 3min+）

![[image-7.webp|269]] 
另一个方法是通过**卡方检验得到的 p 值**来选择 K。
如果 p 值低于设定的显著水平（一般 $\alpha = 0.05$）则拒绝原假设，认为特征和标签之间有相关性。

通过实例化 `chi2` 可以得到 p 值。
```py
chi_value, p_value = chi2(x_fsvar, Y)

k = np.sum(p_value < 0.05)
```
可以发现所有的 p 值都小于设定的显著水平，因此不需要再用相关性过滤了。如果实际中 k 不是全部的特征数量，则在卡方过滤时可以设定参数 `K=k` 达到最佳效果。

##### F 检验
ANOVA，方差检验。用来捕捉**特征和标签之间的线性关系**的过滤方法。有两个类：`feature_selection.f_classif`（分类）和 `feature_selection.regression`（回归）分别用于处理离散型和连续性的数据。都需要配合 `SelectKBest` 使用，当数据服从正态分布时，效果会比较稳定。

F 检验的本质是寻找两组数据之间的**线性关系**，返回的是 F 统计量和 p 值，希望找到 p 值小于显著水平的特征，表明这些特征和标签是显著线性相关的。``
```py
from sklearn.feature_selection import f_classif

F , p_value_F = f_classif(x_fsvar, Y)
np.sum(p_value_F < 0.05)
```

##### 互信息法
如何检测特征和标签之间的非线性关系？**互信息法**可以捕捉每个特征和标签之间的任意关系，包括两个类：`feature_selection.mutual_info_classif` 和 `feature_selection.mutual_info_regression`

**注意**：返回的是每个特征和标签之间的互信息量的估计，范围在[0,1]之间，越靠近 1 说明两者完全相关。
```py
from sklearn.feature_selection import mutual_info_classif as MIC
result = MIC(x_fsvar, Y)
result
```
结果中所有特征的互信息量都不为 0，表明和标签相关。 

### 嵌入法
嵌入法是通过算法的表现来选择特征的方法，即特征的选择和算法的训练同时进行。在训练时得到各个特征的权值系数，根据权值系数的大小选择特征，即考虑各个特征对模型的贡献。因此不管是需要通过方差过滤的特征还是通过相关性过滤的特征都会因为对模型没有贡献而被删去。

但是嵌入法的一个缺点是没有相关的统计理论对权值系数的阈值有规定，当得出大量的特征都有贡献时，难以确定一个合适的阈值筛选一部分特征。因此阈值就是一个超参数，需要通过学习曲线来判断每个模型合适的阈值。

相关模块：`feature_selection.SelectFromModel`，可以搭配任何在拟合后具有 `coef_`，`feature_importances_` 或惩罚项（l1,l2 正则惩罚项）的评估器使用。


如在树模型中有 `feature_importances_` 属性，设定阈值后，重要性低于改阈值的特征就会被移除。

带惩罚项的模型中，正则化的惩罚项越大（如 Lasso），更多特征对应的系数越小，以至于会被压缩到 0，对于那些容易变成 0 的系数，可以筛去。

重要的参数：
- `estimator`：使用的模型，需要有 `feature_importances_` 或 `coef_` 属性或者惩罚项
- `threshold`：阈值

先设定一个阈值 0.05 ，
```py
from sklearn.ensemble import RandomForestClassifier as RFC
from sklearn.feature_selection import SelectFromModel

RFC_ = RFC(n_estimators=10, random_state=42)
X_embedded = SelectFromModel(RFC_, threshold=0.005).fit_transform(X, Y)
X_embedded.shape
```
发现只选出了很少的特征，说明阈值选取的过大了。那么改如何确定一个合适的阈值呢？

```py
max(RFC_.fit(X, Y).feature_importances_)
```
特征重要性的最大值为 0.17，设定一系列低于此重要性的阈值，检测模型的效果：
```py
thresholds = np.linspace(0, max(RFC_.fit(X, Y).feature_importances_), 15)

score = []
for threshold in thresholds:
    x_embedded = SelectFromModel(RFC_, threshold=threshold).fit_transform(X, Y)
    s = cross_val_score(RFC_, x_embedded, Y, cv=5).mean()
    score.append(s)
    
plt.plot(thresholds, score)
plt.show()
```
函数的曲线时单调下降的
![[image-8.webp|334]]

可以确定一个大概的阈值，在改阈值附近进一步搜索合适的阈值，这里选 1，2 个阈值作为搜索范围的边界，
```py
selected_thresholds = thresholds[1:3]
score2 = []
for t in np.linspace(selected_thresholds[0], selected_thresholds[1], 6):
    x_embedded = SelectFromModel(RFC_, threshold=t).fit_transform(X, Y)
    s = cross_val_score(RFC_, x_embedded, Y, cv=5).mean()
    score2.append(s)
```

![[image-9.webp|345]]
可以确定最佳的表现为 0.9376。进一步地，可以调整随机森林地参数，进一步提升模型表现：
```py
RFC_1 = RFC(n_estimators=100, random_state=42)
x_embedded = SelectFromModel(RFC_1, threshold=selected_thresholds[0]).fit_transform(X, Y)
cross_val_score(RFC_1, x_embedded, Y, cv=5).mean()
```
模型的表现提升至了 0.9615。

嵌入法比起方差过滤，选取的特征数更少，并且模型的表现也越好，但是过滤法的计算更快，在大型的数据中，优先考虑过滤法。


### 包装法
和嵌入法十分相似。选取特征的算法不是使用的模型，而是一个特定的算法。通过一个目标函数不断递归地计算每个属性的重要性，再从当前的特征中修建最不重要的特征，直到特征数达到所需的特征数。有一种每次计算的特征不断减少，因此计算成本比嵌入法小。

这里的目标函数是一个专门的数据挖掘算法，核心时找到**最佳特征子集**。

一个典型的目标函数是**递归特征消除法**（Recursive Feature Elimination, RFE），模块 `sklearn.feature_selection.RFE`,
参数：
- `estimator` *实例化*后的评估器
- `n_features_to_select` 要选择的特征个数
- `step` 每次迭代中*希望移除的特征个数*

重要的属性：
- `support_` 特征是否被选中的 Bool 矩阵。
- `ranking_` 特征的综合重要性排名。

```py
from sklearn.feature_selection import RFE

selector = RFE(RFC_, n_features_to_select=277, step=50).fit(X, Y)
```
在筛选后特征后，查看特征的选取情况：
```py
selector.support_
```
可以 `sum(selector.support_)` 验证筛选的特征数

```py
selector.ranking_
```
可以查看每个特征的重要程度，有重要性相同的。

再通过学习曲线选择合适的特征数：
```py
scores = []

for i in range(1, 301, 50):
    x_wrapper = RFE(RFC_, n_features_to_select=i, step=50).fit_transform(X, Y)
    s = cross_val_score(RFC_, x_wrapper, Y, cv=5).mean()
    scores.append(s)

plt.plot(range(1, 301, 50), scores)
plt.show()
```
（需要 6min+）

![[image-10.webp|338]]
可以发现，如果选择 51 作为特征总数，模型的表现可以达到 0.9 以上，缩减了约 94% 的特征同时能够达到比较好的准确率，足以体现包装法的*高效性*。

在同样的特征数下，包装法的运行时间比嵌入法更少，并且两者的模型表现接近。

### 总结
如上的每一种方法都涉及不同的超参数调整。从经验来说
- 过滤法更快速但是比较粗糙
- 包装法和嵌入法更加精确

因此在数据量比较大时，优先选择方差过滤和互信息法初步选择特征，逻辑回归优先选择嵌入法；SVM 优先选择包装法。

进阶：特征创造能找到更高级的特征，进一步提升模型的极限，但是比较困难，比较少用。