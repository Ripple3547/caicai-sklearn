## 概述
逻辑回归是一种线性的分类器。

一般的线性回归问题：
$$
z = \theta ^{\top} x = \theta_{0} + \theta_{1}x_{1} + \cdots  \theta_{n} x_{n}
$$
其中 $\theta_{0}$ 称为截距，$\theta_{1}$~$\theta_{n}$ 称为系数。
任务在于构造一个预测函数 z 映射输入的特征矩阵和标签 y 的线性关系。

对于满足 0-1 分布的离散型变量，可以引入联系函数，将函数 g(z) 分布调整在 0 和 1 之间。对于逻辑回归来说，该联系函数为 Sigmoid 函数：
$$
g(z) = \frac{1}{1 + e^{-z}}
$$

当 z 趋近正无穷时，g(z)趋近于 1；当 z 趋近于负无穷时，g(z)趋近于 0，能够将任何实数映射到(0,1)区间，可将任意值函数转换为更适合二分类的函数。

二元逻辑回归模型的一般形式为
$$
g(z) = y(x) = \frac{1}{1 + e^{-\theta ^{\top} x}}
$$
定义形似几率 $\frac{y(x)}{1 - y(x)}$，取对数可得
$$
\ln  \frac{y(x)}{1 - y(x)} = \theta ^{\top} x
$$
因此 y(x)的形似几率取对数的本质对应的是线性回归。

---
- 逻辑回归对线性关系的拟合效果好，主要用在金融领域
- 拟合和计算快，计算效率高
- 返回的分类结果并不是固定的 0 和 1，而是以小数呈现的类概率数字。可以把逻辑回归返回的结果当作连续性数据来利用

此外逻辑回归还有抗噪声能力强的优点，在小数据集上的表现更好。


## `linear_model.LogisticRegression`
### 二元逻辑回归的损失函数
逻辑回归基于训练数据求解参数 $\theta$，希望模型尽可能拟合训练数据。

使用**损失函数**衡量参数为 $\theta$ 的模型在拟合训练集所产生的信息损失的大小，以衡量参数 $\theta$ 的优劣。

在求解参数 $\theta$ 时追求损失函数最小，让模型在训练数据上的拟合效果最优，预测准确率尽量靠近 100%。

逻辑回归的损失函数可由极大似然估计推导：
$$
J(\theta) = - \sum_{i=1}^{m} y_{i} * \log  y_{\theta}(x_{i}) + (1 - y_{i}) * \log  (1 - y_{\theta}(x_{i}))
$$
其中
- $\theta$ 是求解出的参数
- m 是总样本个数
- $y_{i}$ 是真实标签
- $y_{\theta}(x_{i})$ 是基于 $\theta$ 计算出的逻辑回归返回值
- $x_{i}$ 是各特征的取值

虽然逻辑回归天生是欠拟合的模型，但还须通过控制过拟合的技术帮助调整模型，具体可通过正则化实现。

---
#### 二元逻辑回归损失函数的数学解释
二元逻辑回归的标签服从伯努利分布。
- 样本标签预测为 1 的概率为：$P_{1} = P(\hat{y}_{i} = 1 \mid x_{i }, \theta) = y_{\theta}(x_{i})$
- 样本标签预测为 0 的概率为：$P_{0} = P(\hat{y}_{i} = 0 \mid x_{i}, \theta) = 1 - y_{\theta}(x_{i})$

则当 $y_{i} = 1$ 时，若 $P_{1} = 1$ ，则表示样本的标签预测和真实值一致。因此当 $y_{i} = 1$ 时，希望 $P_{1}$ 接近 1，而 $y_{i} = 0$ 时，$P_{0}$ 接近 1.整合可得
$$
P( \hat{y}_{i}, \mid x_{i}, \theta) = P_{1}^{y_{i}} * P_{0}^{1 - y_{i}}
$$
为了让模型拟合好，损失最小目的，希望 $P( \hat{y}_{i} \mid x_{i}, \theta)$ 为 1（即追求最大值）

定义如下等式表达所有样本在 X 和 $\theta$ 组成的预测函数中，预测出所有的 $\hat{y}$ 的概率：
$$
\begin{align}
P  & = \prod_{i=1}^{m} P( \hat{y}_{i} \mid x_{i}, \theta) \\
\log  P  &  = \sum_{i=1}^{m} (y_{i} * \log  y_{\theta}(x_{i}) + (1-y_{i}) * \log (1 - y_{\theta}(x_{i})))
\end{align}
$$
该函数即交叉熵函数。将极大值问题转换为极小值问题，可得
$$
J(\theta) = - \log P = -\sum_{i=1}^{m} (y_{i} * \log  y_{\theta}(x_{i}) + (1-y_{i}) * \log (1 - y_{\theta}(x_{i})))
$$

### 重要参数 penalty & C
#### 正则化
分为 L1 正则化和 L2 正则化
- L1：$J(\theta)_{L1} = C * J(\theta) + \sum_{j=1}^{n} \lvert \theta_{j} \rvert$
- L2：$J(\theta)_{L2} = C * J(\theta) + \sqrt{ \sum_{j=1}^{n} (\theta_{j})^{2} }$

参数设置：
- `penalty`：输入 `l1` 或 `l2` 指定正则化方式，默认 `l2`。若使用 `l1`，参数 `solver` 中只能使用 `liblinear` 和 `saga`
- `C`：正则化强度的倒数，默认为 1，大于 0 的浮点数。C 越小，损失函数越小，惩罚越重，正则化的效力越强

当正则化强度增大时，参数 $\theta$ 的变化不同，使用 L1 正则化会将参数压缩为 0；L2 正则化只会让参数尽量小，不会取到 0.

L1 正则化逐渐加强的过程中，携带信息量小的、对模型贡献不大的特征的参数会更先变成 0，因此 L1 正则化本质是一个特征选择过程，掌握了参数的稀疏性。

通常为了防止过拟合，可以选择 L2 正则化。如果选择 L2 正则化后还是过拟合，模型在未知数据上的表现差，则可以考虑 L1 正则化。

#### 逻辑回归中的特征工程
- 可以使用算法帮助筛选特征，再根据业务选择更少的特征
- PCA 和 SVD 一般不用
- 统计方法可以使用：过滤法可以用在逻辑回归上。多重共线性对线性模型有一定影响，如果模型表现受到共线性影响，可以使用方差膨胀因子 VIF 消除
- 高效的嵌入法 embedded：L1 正则化可以做特征选择，结合嵌入法的模块，可以筛选出让模型更高效的特征。此时不考虑训练集和测试集，所有数据放入模型进行降维。调参角度包括 threshold 和 C
- 比较麻烦的系数累加法：对特征系数 `coef_` 从大到小排序，累加绘制曲线。需要知道特征的位置对应关系
- 包装法：直接设定需要的特征个数，适合*需要若干个变量*的现实需求。


### 梯度下降：重要参数 `max_iter`
逻辑回归的损失函数为
$$
J(\theta) = - \sum_{i=1}^{m}(y_{i} * \log  y_{\theta}(x_{i}) + (1 - y_{i} )* \log (1 - y_{\theta}(x_{i})))
$$
梯度向量的表示形式为：
$$
\frac{ \partial  }{ \partial \theta_{j} } J(\theta) = d_{j} = \sum_{i=1}^{m}(y_{\theta}(x_{i}) - y_{i}) x_{ij}
$$

迭代方程：
$$
\theta_{j+1} = \theta_{j} - \alpha * d_{j} = \theta_{j} - \alpha* \sum_{i=1}^{m}(y_{\theta}(x_{i}) - y_{i}) x_{ij}
$$

设置 `max_iter` 最大迭代次数代替步长，当 `max_iter` 越大，步长越小，模型迭代时间越长。当 `max_iter` 限制的步数走完了，损失函数的最小值还没有找到，说明 $\theta$ 没有收敛，会弹出警告，当 `max_iter` 增大，损失函数值能减小，但模型可能会过拟合，因此即使达到 `max_iter`，模型的训练效果已经不错，就不再需要增大 `max_iter` 了。

### 二元回归与多元回归：重要参数 solver & multi_class
- 某种分类看作 1，其他分类都为 0，称为一对多，简称 OvR
- 若干分类划为 1，剩下的几个分类划为 0 值，称为多对多，简称 MvM

sklaern 中可用 `multi_class` 告知预测标签的类型：
- `ovr` 表示二分类或用一对多的形式处理多分类问题
- `multinomial` 表示多分类问题，在 `solver=liblinear` 时不可用
- `auto` 表示根据数据的分类情况和其他参数确定模型要处理的分类问题的类型

`solver` 可以控制求解器，一共 5 种选择：
- `liblinear`：坐标下降法，二分类专用。默认
- `lbfgs`：拟牛顿法的一种
- `newton-cg`：牛顿法的一种
- `sag`：随机平均梯度下降
- `saga`：随机平均梯度下降的进化

### 样本不平衡与参数 `class_weight`
样本不平衡是指数据集中一类标签天然占很大比例，或误分类的代价很高。
如：
- 分类潜在罪犯和普通人，未识别出潜在罪犯的代价会很高，而将普通人误识别为潜在犯罪者的代价相对较小
- 判断一个客户是否会违约，真正违约的人非常少，模型全识别为不会违约的正确率也很高

`class_weight` 可以对样本标签进行一定的均衡，给少量的标签更多权重。默认为 None。使用 `balanced` 模式可以对标签进行均衡。

通常该参数不易通过学习曲线评估参数效果，常见的处理样本不平衡的方法有采样法（上采样或下采样）。对逻辑回归，**上采样**是最好的办法。

## 评分卡案例
### 预处理
家属数量的缺失比较少，不重要，可以删除缺失行。这里采用均值填充。

月收入字段比较重要，不能删除。对于月收入的填充，猜测是低收入人群，但实际中缺失的原因比较多样，需要和业务人员沟通，这里采用随机森林填充。
- 特征 T 不缺失的其他 n-1 个特征和本来的标签：`X_train`
- 特征 T 不缺失的值：`Y_train`
- 特征 T 缺失的值对应的其他 n-1 个特征和本来的标签：`X_test`
- 特征 T 缺失的值：需要预测， `Y_test`

---
- 异常值的处理：需要根据具体业务判断，通常用箱线图或 3 $\sigma$ 法则找出。发现两年内逾期 90 天的有 98 次的客户，但对应标签有 0 也有 1。违约 90 天的次数在 90 次以上的客户一共有 225 个
- 量纲不统一：由于评分卡是面向业务人员的，尽量不做标准化等操作保持数据原貌，通过调参优化模型效果.
- 处理偏态分布：`imblearn` 类，经过实例化、`fit` 拟合，处理不平衡数据集。

---
### 分箱
通过信息量衡量特征的分箱是否合适
$$
IV = \sum_{i=1}^{N}(good \% - bad \%) * WOE_{i}
$$
其中 $WOE_{i} = \ln\left( \dfrac{good\%}{bad \%} \right)$ 
- $N$ 是总分箱数
- $good \%$ 是某个箱内的优质客户（标签 0）占所有优质客户的比例
- $bad \%$ 是箱内的坏客户占特征中所有坏客户的比例
- WOE 是衡量违约概率的指标
![[image-11.webp]]
IV 代表了特征的信息量以及该特征对模型的贡献，IV 比较低，说明有效信息少，对模型的贡献度较低；若 IV 非常大，有效信息非常多，对模型的贡献超高并且可疑。IV 值并非越大越好，利用 IV 值的曲线找到合适的分箱个数。

分箱需要使得组间差异大，组内差异小，通过卡方检验对比两个箱子之间的相似性，若卡方值小，则箱子相似，可以合并。
因此，对特征进行分箱的步骤为：
- 将连续性变量分成数量较多的分类型变量
- 确保每组包含两种类别的样本，从而计算 IV
- 对相邻的组进行卡方检验，p 值大的组合并，知道数据的组数小于设定的 N 箱
- 重复设定不同的分箱数，绘制对应的 IV 值学习曲线，找到最佳的分箱数
- 分箱后计算每箱的 WOE 值、$bad\%$，观察分箱效果

最后对各个特征进行分箱，观察每个特征的 IV 值，挑选特征。

---
### 卡方检验
检验两个相邻箱之间的卡方置信度，若 p 值大，则两个箱相似，合并。

```py
IV = []
axisx = []
# 计算相关度


while len(num_bins_) > 2:
    pvs = []
    for i in range(len(num_bins_) - 1):
        x1 = num_bins_[i][2:]
        x2 = num_bins_[i+1][2:]
        
        p = scipy.stats.chi2_contingency([x1,x2])[1]
        pvs.append(p)
    
    # p值最大的两组合并
    i = pvs.index(max(pvs))
    num_bins_[i:i+2] = [
        (
            num_bins_[i][0],
            num_bins_[i+1][1],
            num_bins_[i][2] + num_bins_[i+1][2],
            num_bins_[i][3] + num_bins_[i+1][3]
        )
    ]
    
    # 计算 IV值
    df = get_woe(num_bins_)
    axisx.append(len(num_bins_))
    IV.append(get_iv(df))
    
```

![[image-12.webp|290]]
根据曲线的变化趋势，箱数为6时的IV值开始骤降，是合适的分箱数。


对所有特征进行分箱，发现有些 IV 值曲线结果特别，原因在于这类特征是分类型特征，不具有连续性特点，只能手动分箱。
```py
auto_col_bins = {"RevolvingutilizationofunsecuredLines":6,
                    "age":5,
                    "DebtRatio":4,
                    "MonthlyIncome":3,
                    "NumberofopencreditLinesAndLoans" : 5
                    }

#不能使用自动分箱的变量
hand_bins = {"NumberofTime30-59DaysPastDueNotWorse": [0, 1,2, 13]
                , "NumberofTimes90DaysLate": [0, 1, 2, 17]
                , "NumberRealEstateLoansorLines": [0, 1,2, 4, 54]
                , "NumberofTime60-89DaysPastDueNotWorse": [0, 1,2, 8]
                , "NumberofDependents": [0, 1, 2, 3]}

#保证区间覆盖使用np.inf替换最大值，用-np.inf替换最小值
hand_bins = {k: [-np.inf,*v[:-1], np.inf] for k,v in hand_bins.items()}
```
为了防止新数据超出分箱边界，需要调整最大值和最小值


计算不能自动分箱的特征的 WOE。最后将原始特征矩阵中每个数据映射为对应的箱的 WOE 值，形成最终的特征矩阵。

### 模型输出
逻辑回归，对 C 和 max_iter 进行调参，发现提升效果有限。绘制 ROC 曲线
![[image-13.webp|335]]

评分卡的计算公式
$$
Score = A - B * \log (odds)
$$
- 其中 $\log(odds)$ 代表了违约的可能性，等价对数几率形式 $\theta ^{\top} x$

常数 A 和 B 可由两个假设的分值代入求出
1. 某个特定违约概率下的预期分数
2. 指定的违约概率翻倍的分数

求得 A 和 B 后，基础分可以由**截距**作为 $\log(odds)$ 计算得出，不受各特征的影响。

最后由每个箱的 WOE 值可计算每个特征的每个分箱的分数 $Score = -WOE * B * Intercept$。
