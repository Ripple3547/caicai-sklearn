## 概述
逻辑回归是一种线性的分类器。

一般的线性回归问题：
$$
z = \theta ^{\top} x
$$
任务在于构造一个预测函数 z 映射输入的特征矩阵和标签 y 的线性关系。

对于满足 0-1 分布的离散型变量，可以引入联系函数，将函数 g(z) 分布调整在 0 和 1 之间。对于逻辑回归来说，该联系函数为 Sigmoid 函数：
$$
g(z) = \frac{1}{1 + e^{-z}}
$$

当 z 趋近正无穷时，g(z)趋近于 1；当 z 趋近于负无穷时，g(z)趋近于 0，能够将任何实数映射到(0,1)区间，可将任意值函数转换为更适合二分类的函数。

二元逻辑回归模型的一般形式为
$$
g(z) = y(x) = \frac{1}{1 + e^{-\theta ^{\top} x}}
$$
定义形似几率 $\frac{y(x)}{1 - y(x)}$，取对数可得
$$
\ln  \frac{y(x)}{1 - y(x)} = \theta ^{\top} x
$$
因此 y(x)的形似几率取对数的本质对应的是线性回归。

---
- 逻辑回归对线性关系的拟合效果好，主要用在金融领域
- 拟合和计算快，计算效率高
- 返回的分类结果并不是固定的 0 和 1，而是以小数呈现的类概率数字。可以把逻辑回归返回的结果当作连续性数据来利用

此外逻辑回归还有抗噪声能力强的优点，在小数据集上的表现更好。


## `linear_model.LogisticRegression`
### 二元逻辑回归的损失函数
逻辑回归基于训练数据求解参数 $\theta$，希望模型尽可能拟合训练数据。

使用**损失函数**衡量参数为 $\theta$ 的模型在拟合训练集所产生的信息损失的大小，以衡量参数 $\theta$ 的优劣。

在求解参数 $\theta$ 时追求损失函数最小，让模型在训练数据上的拟合效果最优，预测准确率尽量靠近 100%。

逻辑回归的损失函数可由极大似然估计推导：
$$
J(\theta) = - \sum_{i=1}^{m} y_{i} * \log  y_{\theta}(x_{i}) + (1 - y_{i}) * \log  (1 - y_{\theta}(x_{i}))
$$
其中
- $\theta$ 是求解出的参数
- m 是总样本个数
- $y_{i}$ 是真实标签
- $y_{\theta}(x_{i})$ 是基于 $\theta$ 计算出的逻辑回归返回值
- $x_{i}$ 是各特征的取值

虽然逻辑回归天生是欠拟合的模型，但还须通过控制过拟合的技术帮助调整模型，具体可通过正则化实现。

---
#### 二元逻辑回归损失函数的数学解释
二元逻辑回归的标签服从伯努利分布。
- 样本标签预测为 1 的概率为：$P_{1} = P(\hat{y}_{i} = 1 \mid x_{i }, \theta) = y_{\theta}(x_{i})$
- 样本标签预测为 0 的概率为：$P_{0} = P(\hat{y}_{i} = 0 \mid x_{i}, \theta) = 1 - y_{\theta}(x_{i})$

则当 $y_{i} = 1$ 时，若 $P_{1} = 1$ ，则表示样本的标签预测和真实值一致。因此当 $y_{i} = 1$ 时，希望 $P_{1}$ 接近 1，而 $y_{i} = 0$ 时，$P_{0}$ 接近 1.整合可得
$$
P( \hat{y}_{i}, \mid x_{i}, \theta) = P_{1}^{y_{i}} * P_{0}^{1 - y_{i}}
$$
为了让模型拟合好，损失最小目的，希望 $P( \hat{y}_{i} \mid x_{i}, \theta)$ 为 1（即追求最大值）

定义如下等式表达所有样本在 X 和 $\theta$ 组成的预测函数中，预测出所有的 $\hat{y}$ 的概率：
$$
\begin{align}
P  & = \prod_{i=1}^{m} P( \hat{y}_{i} \mid x_{i}, \theta) \\
\log  P  &  = \sum_{i=1}^{m} (y_{i} * \log  y_{\theta}(x_{i}) + (1-y_{i}) * \log (1 - y_{\theta}(x_{i})))
\end{align}
$$
该函数即交叉熵函数。将极大值问题转换为极小值问题，可得
$$
J(\theta) = - \log P = -\sum_{i=1}^{m} (y_{i} * \log  y_{\theta}(x_{i}) + (1-y_{i}) * \log (1 - y_{\theta}(x_{i})))
$$

### 重要参数 penalty & C
#### 正则化
分为 L1 正则化和 L2 正则化
- L1：$J(\theta)_{L1} = C * J(\theta) + \sum_{j=1}^{n} \lvert \theta_{j} \rvert$
- L2：$J(\theta)_{L2} = C * J(\theta) + \sqrt{ \sum_{j=1}^{n} (\theta_{j})^{2} }$

参数设置：
- `penalty`：输入 `l1` 或 `l2` 指定正则化方式，默认 `l2`。若使用 `l1`，参数 `solver` 中只能使用 `liblinear` 和 `saga`
- `C`：正则化强度的倒数，默认为 1，大于 0 的浮点数。C 越小，损失函数越小，惩罚越重，正则化的效力越强

当正则化强度增大时，参数 $\theta$ 的变化不同，使用 L1 正则化会将参数压缩为 0；L2 正则化只会让参数尽量小，不会取到 0.

L1 正则化逐渐加强的过程中，携带信息量小的、对模型贡献不大的特征的参数会更先变成 0，因此 L1 正则化本质是一个特征选择过程，掌握了参数的稀疏性。

通常为了防止过拟合，可以选择 L2 正则化。如果选择 L2 正则化后还是过拟合，模型在未知数据上的表现差，则可以考虑 L1 正则化。