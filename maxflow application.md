## Disjoint Path Problem
给定一个图，一个源点和汇点，找到其中的最大的边不相交路径的数量。

如果有两条路径，且没有公共的边，则这两条路径时 edge-disjoint 的。

可以转化为最大流问题，由于已经有了源点和汇点，因此只需要设定流量，对每条边设定最大容量为 1。如果有 k 条互不相交的路径，则存在一个流值为 k 的网络流

Menger 定理：最大互不相交路径的条数 k 等于最小割边集的个数

