模块 `sklearn.decomposition.PCA`
## 降维如何实现
略

## `n_components`
- 降维后的特征数，一般为 `[0, min(X.shape)]` 之间。
- 若 `n_components="mle"`，则会用最大似然估计自动选择特征数。
- 也可按信息量占比选择：`n_components=0.5, svd_solver="full"`

`PCA().fit()` 的属性：
- `explained_variance_` 降维后每个新特征向量上所带的信息量大小（可解释方差的大小）
- `explained_variance_ratio_` 每个新特征向量所占信息量占原始总信息量的百分比



## PCA 中的 SVD
SVD 的分解结果为 $X = U\Sigma V^{\top}$，其中的 $V^{\top}$ 就是特征空间，选择 `n_components` 后会选取前 k 行，则降维后的特征矩阵为 `X_dr = X * V[:k]^T` 

可通过 `PCA().fit().components_` 查看 `V(k,n)`。

### `svd_solver`，`random_state`
`svd_solver` 的参数：
- `auto` 基于 shape 和 `n_components` 选择分解器。若输入尺寸大于 500x500，提取的特征数小于 `min(X.shape)` 的 80%，则用 `randomized` 方法。否则计算精确的 SVD
- `full` 从 `scipy.linalg.svd` 调用 LAPACK 分解器生成精确 SVD。
- `arpack` 从 `scipy.sparse.linalg.svds` 调用 ARPACK 分解器运行截断 SVD。一般用于稀疏矩阵情况。
- `randomized` 生成多个随机向量，基于随机向量构建后续的向量空间，适合矩阵巨大的情况。

### `components_`
新特征向量组成的新特征空间就是 `V(k,n)`，可以通过可视化该矩阵比较新特征矩阵从原始数据中提取了什么信息。

在人脸识别例子中，新特征空间代表了和五官、亮度相关的向量，从原始数据中的五官和亮度信息中提取

## `inverse_transform`
X 右乘 `V(k,n)` 后可以生成新特征矩阵，同理可以将新特征矩阵还原为 X。

由于降维过程中部分信息被舍弃，因此 `X_dr` 中不会包含原数据的全部信息，降维不是完全可逆的。

**噪音过滤**：有效信息的特征的方差远大于噪音的方差，`inverse_transform` 可以在不恢复原始数据的情况下，将降维后的数据返回到原本的高维空间，保证维度而去掉方差很小的特征所带的信息。
